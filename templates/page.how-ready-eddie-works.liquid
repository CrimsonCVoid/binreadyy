{% layout 'none' %}
{%- comment -%}
  Ready Eddie - How It Works
  --------------------------
  Standalone Shopify page template. Reached via QR code on
  in-store packaging or shelf signage. Single page, scannable, with one
  interactive moment that demonstrates what the product does.

  Setup
  -----
  1. Place this file at: templates/page.how-ready-eddie-works.liquid
  2. Place these images in your theme's /assets folder (already named):
       cuff-attached.jpeg        - Eddie hanging in DOWN position
       eddie-flipped-up.jpeg     - Eddie in UP position (arm raised)
       cuff-installed-empty.jpeg - Cuff hardware close-up
       bin-a-lid-raised.jpeg     - Bin A side profile (lid sits high)
       bin-b-lid-level.jpeg      - Bin B side profile (lid sits flush)
  3. In Shopify admin -> Pages -> create/edit a page and set
     "Theme template" to: how-ready-eddie-works
  4. The {% layout 'none' %} directive above bypasses theme.liquid so
     this page renders as a standalone HTML document.
{%- endcomment -%}
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
<meta name="theme-color" content="#F5612A" />
<title>{{ page.title | default: 'How Ready Eddie works' }}</title>
{%- if page.content != blank -%}
  <meta name="description" content="{{ page.content | strip_html | truncate: 160 | escape }}" />
{%- else -%}
  <meta name="description" content="Ready Eddie clips onto the hinge of your bin. When the truck flips your bin, Eddie flips up. See him from the porch. That's the whole product." />
{%- endif -%}

<link rel="canonical" href="{{ shop.url }}{{ page.url }}" />
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,500;12..96,700;12..96,800&family=Fraunces:ital,opsz,wght@1,9..144,400;1,9..144,500&family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600&display=swap" rel="stylesheet" />

{{ content_for_header }}

<style>
:root {
  /* Brand palette — kept in sync with assets/theme.css so the
     standalone how-it-works page reads as the same brand. */
  --cream: #FAF8F2;
  --cream-deep: #F1EDE0;
  --orange: #F5612A;
  --orange-deep: #D94F1E;
  --orange-soft: #FEEFE6;
  --bin-green: #3D6B4F;
  --ink: #1A1A17;
  --ink-soft: #44443F;
  --ink-muted: #64645C;
  --rule: #E3DFCF;
  --rule-strong: #C8C3AE;
}

* { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

html { scroll-behavior: smooth; }

body {
  font-family: 'DM Sans', system-ui, sans-serif;
  font-size: 17px;
  line-height: 1.55;
  color: var(--ink);
  background: var(--cream);
  -webkit-font-smoothing: antialiased;
  overflow-x: hidden;
  min-height: 100vh;
}

img { max-width: 100%; display: block; }

.wrap { max-width: 560px; margin: 0 auto; padding: 0 22px; }

.display {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 800;
  letter-spacing: -0.035em;
  line-height: 0.92;
  font-variation-settings: "wdth" 95;
}
.serif {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-weight: 400;
  letter-spacing: -0.01em;
}
.eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--ink-soft);
}
.eyebrow::before {
  content: '';
  width: 26px;
  height: 1px;
  background: var(--orange);
  flex-shrink: 0;
}
.bins .eyebrow::before { background: var(--cream); opacity: 0.55; }

/* ---------- TOP BAR (mirrors the homepage site-header treatment) ---------- */
.top {
  position: sticky;
  top: 0;
  z-index: 10;
  background: rgba(250, 248, 242, 0.86);
  backdrop-filter: saturate(180%) blur(18px);
  -webkit-backdrop-filter: saturate(180%) blur(18px);
  border-bottom: 1px solid transparent;
  transition: background-color 0.4s ease, border-color 0.4s ease, padding 0.4s ease;
}
.top.scrolled {
  background: rgba(250, 248, 242, 0.95);
  border-bottom-color: var(--rule);
}
.top-inner {
  max-width: 1180px;
  margin: 0 auto;
  padding: 14px 28px;
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: 16px;
}
.top-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 9px 14px;
  border-radius: 8px;
  text-decoration: none;
  color: var(--ink-soft);
  font-family: 'DM Sans', sans-serif;
  font-size: 14px;
  font-weight: 600;
  letter-spacing: -0.005em;
  line-height: 1;
  transition: color 0.3s ease, background 0.3s ease;
  white-space: nowrap;
}
.top-back:hover,
.top-back:focus-visible {
  color: var(--ink);
  background: rgba(21,21,19,0.04);
  outline: none;
}
.top-back .top-back-arrow {
  font-size: 15px;
  line-height: 1;
  transition: transform 0.3s ease;
}
.top-back:hover .top-back-arrow { transform: translateX(-2px); }

.top-nav {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 4px;
}
.top-nav a {
  text-decoration: none;
  color: var(--ink-soft);
  font-family: 'DM Sans', sans-serif;
  font-size: 14.5px;
  font-weight: 500;
  padding: 10px 14px;
  border-radius: 8px;
  line-height: 1;
  transition: color 0.3s ease, background 0.3s ease;
}
.top-nav a:hover,
.top-nav a:focus-visible {
  color: var(--ink);
  background: rgba(21,21,19,0.04);
  outline: none;
}

.top-buy {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 11px 20px;
  border-radius: 10px;
  background: var(--orange);
  color: var(--cream);
  font-family: 'DM Sans', sans-serif;
  font-weight: 600;
  font-size: 14px;
  letter-spacing: -0.005em;
  line-height: 1;
  text-decoration: none;
  white-space: nowrap;
  box-shadow:
    0 1px 0 rgba(255,255,255,0.35) inset,
    0 10px 24px -12px rgba(245,97,42,0.55);
  transition: background 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
}
.top-buy:hover,
.top-buy:focus-visible {
  background: var(--orange-deep);
  transform: translateY(-1px);
  box-shadow:
    0 1px 0 rgba(255,255,255,0.4) inset,
    0 16px 30px -14px rgba(217,79,30,0.6);
  outline: none;
}

@media (max-width: 720px) {
  .top-inner { grid-template-columns: auto auto; padding: 12px 18px; }
  .top-nav { display: none; }
}
@media (max-width: 420px) {
  .top-back .top-back-label { display: none; }
  .top-back { padding: 9px 10px; }
  .top-buy { padding: 10px 16px; }
}

/* ---------- HERO ---------- */
.hero {
  padding: 54px 0 30px;
  text-align: left;
}
.hero .eyebrow { margin-bottom: 18px; }
.hero h1 {
  font-size: clamp(46px, 12vw, 64px);
  margin-bottom: 22px;
}
.hero h1 .accent { color: var(--orange); }
.hero h1 .it {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-weight: 400;
  font-variation-settings: "SOFT" 100, "WONK" 1;
}
.hero p {
  font-size: 19px;
  color: var(--ink-soft);
  line-height: 1.45;
  max-width: 460px;
}

/* ---------- DEMO ---------- */
.demo {
  padding: 40px 0 50px;
}
.demo-stage {
  position: relative;
  aspect-ratio: 4/5;
  background: var(--bin-green);
  border-radius: 22px;
  overflow: hidden;
  box-shadow: 0 24px 50px -20px rgba(26,22,18,0.4);
  margin-bottom: 22px;
}
.demo-stage img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: opacity 0.55s cubic-bezier(0.4, 0, 0.2, 1);
}
.demo-stage img.up { opacity: 0; }
.demo-stage[data-state="up"] img.down { opacity: 0; }
.demo-stage[data-state="up"] img.up { opacity: 1; }

.demo-state-label {
  position: absolute;
  top: 18px; left: 18px;
  background: var(--cream);
  padding: 10px 16px;
  border-radius: 100px;
  font-size: 12px;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: all 0.4s ease;
}
.demo-state-label .dot {
  width: 8px; height: 8px;
  background: var(--ink-soft);
  border-radius: 50%;
}
.demo-stage[data-state="down"] .demo-state-label {
  background: var(--orange);
  color: var(--cream);
}
.demo-stage[data-state="down"] .demo-state-label .dot {
  background: var(--cream);
  box-shadow: 0 0 0 0 rgba(251,241,222, 0.7);
  animation: pulse 1.6s infinite;
}
@keyframes pulse {
  0% { box-shadow: 0 0 0 0 rgba(251,241,222, 0.7); }
  70% { box-shadow: 0 0 0 10px rgba(251,241,222, 0); }
  100% { box-shadow: 0 0 0 0 rgba(251,241,222, 0); }
}

.demo-state-label .text-down,
.demo-state-label .text-up { display: none; }
.demo-stage[data-state="down"] .demo-state-label .text-down { display: inline; }
.demo-stage[data-state="up"] .demo-state-label .text-up { display: inline; }

.demo-button {
  width: 100%;
  background: var(--ink);
  color: var(--cream);
  border: none;
  padding: 22px 24px;
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 700;
  font-size: 18px;
  letter-spacing: -0.01em;
  border-radius: 100px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  transition: all 0.2s ease;
}
.demo-button:hover, .demo-button:active {
  background: var(--orange);
  transform: translateY(-1px);
}
.demo-button .arrow {
  display: inline-block;
  transition: transform 0.4s ease;
}

.demo-controls { display: block; }

@media (min-width: 640px) {
  .demo .wrap {
    max-width: 880px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    align-items: center;
  }
  .demo-stage { margin-bottom: 0; aspect-ratio: 4/5; }
  .demo-controls { display: flex; flex-direction: column; gap: 14px; }
  .demo-controls .demo-caption { text-align: left; margin-top: 0; }
}

.demo-caption {
  margin-top: 18px;
  text-align: center;
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 17px;
  color: var(--ink-soft);
}

/* ---------- BIG STATEMENT ---------- */
.statement {
  padding: 60px 0 70px;
  background: var(--cream-deep);
  margin: 0 -100vw;
  padding-left: calc(100vw + 22px);
  padding-right: calc(100vw + 22px);
}
.statement-inner { max-width: 560px; margin: 0 auto; }
.statement p {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 500;
  font-size: 26px;
  line-height: 1.2;
  letter-spacing: -0.02em;
}
.statement p .hl {
  background: linear-gradient(transparent 65%, rgba(255,90,31,0.4) 65%);
  padding: 0 4px;
}
.statement p + p { margin-top: 20px; }

/* ---------- HOW IT WORKS ---------- */
.steps {
  padding: 60px 0 30px;
}
.steps h2 {
  font-size: clamp(36px, 9vw, 48px);
  margin-bottom: 36px;
}
.step {
  display: grid;
  grid-template-columns: 70px 1fr;
  gap: 18px;
  padding: 24px 0;
  border-bottom: 1px solid var(--rule);
}
.step:last-child { border-bottom: none; }
.step-num {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 800;
  font-size: 56px;
  line-height: 0.85;
  color: var(--orange);
  letter-spacing: -0.04em;
  font-variation-settings: "wdth" 75;
}
.step h3 {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 21px;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 6px;
  line-height: 1.15;
}
.step p {
  color: var(--ink-soft);
  font-size: 16px;
  line-height: 1.5;
}

/* ---------- MECHANISM ---------- */
.mech {
  padding: 50px 0 60px;
}
.mech-card {
  background: var(--ink);
  color: var(--cream);
  border-radius: 18px;
  overflow: hidden;
}
.mech-card .photo {
  aspect-ratio: 4/3;
  overflow: hidden;
}
.mech-card .photo img { width: 100%; height: 100%; object-fit: cover; }
.mech-card .body { padding: 28px 26px 32px; }
.mech-card .eyebrow {
  color: var(--orange);
  margin-bottom: 12px;
}
.mech-card h3 {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 26px;
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.1;
  margin-bottom: 14px;
}
.mech-card p {
  color: rgba(251,241,222,0.85);
  font-size: 15px;
  line-height: 1.6;
}
.mech-card p + p { margin-top: 12px; }

/* ---------- BINS ---------- */
.bins {
  padding: 60px 0 50px;
  background: var(--bin-green);
  color: var(--cream);
  margin: 0 -100vw;
  padding-left: calc(100vw + 22px);
  padding-right: calc(100vw + 22px);
}
.bins-inner { max-width: 1080px; margin: 0 auto; }
.bins .eyebrow { color: rgba(251,241,222,0.6); margin-bottom: 16px; }
.bins h2 {
  font-size: clamp(36px, 9vw, 48px);
  margin-bottom: 14px;
  color: var(--cream);
}
.bins .lead {
  color: rgba(251,241,222,0.85);
  font-size: 17px;
  margin-bottom: 32px;
  max-width: 560px;
}

.bins-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 22px;
  align-items: stretch;
}
@media (max-width: 720px) {
  .bins-grid { grid-template-columns: 1fr; gap: 18px; }
}

.bin {
  background: var(--cream);
  color: var(--ink);
  border-radius: 16px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}
.bin .photo {
  aspect-ratio: 5/3;
  overflow: hidden;
}
.bin .photo img { width: 100%; height: 100%; object-fit: cover; }
.bin .body { padding: 22px 22px 26px; }
.bin .tag {
  display: inline-block;
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 800;
  font-size: 12px;
  letter-spacing: 0.14em;
  background: var(--ink);
  color: var(--cream);
  padding: 5px 10px;
  border-radius: 4px;
  margin-bottom: 14px;
}
.bin.b .tag { background: var(--orange); }
.bin h3 {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-size: 22px;
  font-weight: 700;
  letter-spacing: -0.02em;
  line-height: 1.15;
  margin-bottom: 10px;
}
.bin .test {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-size: 15px;
  color: var(--ink-soft);
  padding-left: 14px;
  border-left: 3px solid var(--orange);
  margin-bottom: 18px;
  line-height: 1.4;
}
.bin .pick {
  display: flex;
  align-items: center;
  gap: 10px;
  font-weight: 600;
  font-size: 15px;
}
.cuff-swatch {
  width: 22px; height: 22px;
  border-radius: 50%;
  flex-shrink: 0;
  border: 1px solid var(--rule);
}
.cuff-swatch.gray { background: #6E6F71; }
.cuff-swatch.black { background: #1A1612; }

/* ---------- OUTRO ---------- */
.outro {
  padding: 70px 0 90px;
  text-align: center;
}
.outro h2 {
  font-size: clamp(36px, 9vw, 50px);
  margin-bottom: 18px;
}
.outro h2 .it {
  font-family: 'Fraunces', serif;
  font-style: italic;
  font-weight: 400;
}
.outro p {
  color: var(--ink-soft);
  font-size: 17px;
  margin-bottom: 32px;
  max-width: 400px;
  margin-left: auto;
  margin-right: auto;
}
.outro .links {
  display: flex;
  flex-direction: column;
  gap: 12px;
}
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 18px 24px;
  font-family: 'DM Sans', sans-serif;
  font-weight: 600;
  font-size: 16px;
  border-radius: 100px;
  text-decoration: none;
  border: 2px solid transparent;
  transition: all 0.2s ease;
  width: 100%;
}
.btn-primary { background: var(--orange); color: var(--cream); border-color: var(--orange); }
.btn-primary:hover { background: var(--orange-deep); border-color: var(--orange-deep); }
.btn-ghost { background: transparent; color: var(--ink); border-color: var(--ink); }
.btn-ghost:hover { background: var(--ink); color: var(--cream); }
.btn.is-disabled { opacity: 0.45; cursor: not-allowed; pointer-events: none; }
.btn.is-disabled small { display: block; font-weight: 400; font-size: 12px; opacity: 0.85; margin-top: 2px; }

/* ---------- FOOTER ---------- */
footer {
  background: var(--ink);
  color: rgba(251,241,222,0.6);
  padding: 30px 22px;
  text-align: center;
  font-size: 13px;
}
footer .foot-logo {
  font-family: 'Bricolage Grotesque', sans-serif;
  font-weight: 800;
  font-size: 22px;
  color: var(--cream);
  letter-spacing: -0.02em;
  margin-bottom: 6px;
}
footer .small {
  font-size: 12px;
  letter-spacing: 0.06em;
  margin-top: 12px;
  color: rgba(251,241,222,0.4);
}

/* Subtle entry animation for hero */
.hero h1, .hero p, .hero .eyebrow {
  opacity: 0;
  transform: translateY(8px);
  animation: rise 0.7s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
}
.hero .eyebrow { animation-delay: 0.05s; }
.hero h1 { animation-delay: 0.15s; }
.hero p { animation-delay: 0.3s; }
@keyframes rise {
  to { opacity: 1; transform: translateY(0); }
}
</style>
</head>

<body>

<!-- TOP -->
<header class="top">
  <div class="top-inner">
    <a href="/" class="top-back" aria-label="Return to front page">
      <span class="top-back-arrow" aria-hidden="true">&larr;</span>
      <span class="top-back-label">Front page</span>
    </a>
    <nav class="top-nav" aria-label="Site sections">
      <a href="/#reviews">Reviews</a>
      <a href="/#faq">FAQ</a>
      <a href="/#help">Contact us</a>
    </nav>
    <a href="/#buy" class="top-buy">Buy</a>
  </div>
</header>
<script>
  (function () {
    var top = document.querySelector('.top');
    if (!top) return;
    function onScroll() {
      if (window.scrollY > 8) top.classList.add('scrolled');
      else top.classList.remove('scrolled');
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  })();
</script>

<!-- HERO -->
<section class="hero">
  <div class="wrap">
    <div class="eyebrow">You scanned the box. Hi.</div>
    <h1 class="display">
      It&rsquo;s a <span class="accent">trash-day</span> <span class="it">signal.</span>
    </h1>
    <p>Eddie clips onto the hinge of your bin. You flip him up before pickup; the truck flips him flat when it empties the bin. A glance from the porch tells you which is which.</p>
  </div>
</section>

<!-- INTERACTIVE DEMO -->
<section class="demo">
  <div class="wrap">
    <div class="demo-stage" id="stage" data-state="up">
      <img src="{{ 'EddyFlippedDown.jpeg' | asset_url }}" alt="Eddie hanging in the down position on a green bin" class="down" loading="eager" />
      <img src="{{ 'EddyFlippedUp.jpeg' | asset_url }}" alt="Eddie flipped up with arm raised after pickup" class="up" loading="eager" />
      <div class="demo-state-label">
        <span class="dot"></span>
        <span class="text-down">Bin emptied</span>
        <span class="text-up">Bin not emptied</span>
      </div>
    </div>
    <div class="demo-controls">
      <button class="demo-button" id="toggle" type="button">
        <span class="arrow">&darr;</span>
        <span class="label">Tap to send the truck</span>
      </button>
      <p class="demo-caption">Tap a few times. That&rsquo;s the whole interaction in real life.</p>
    </div>
  </div>
</section>

<!-- BIG STATEMENT -->
<section class="statement">
  <div class="statement-inner">
    <p>You&rsquo;re standing at the front window <span class="hl">squinting at a closed lid</span>, trying to figure out if the truck came yet.</p>
    <p class="serif" style="color: var(--ink-soft);">Eddie ends that.</p>
  </div>
</section>

<!-- HOW (3 STEPS) -->
<section class="steps">
  <div class="wrap">
    <div class="eyebrow" style="margin-bottom: 14px;">The mechanism</div>
    <h2 class="display">No batteries. <span class="serif">No app.</span></h2>

    <div class="step">
      <div class="step-num">01</div>
      <div>
        <h3>Bin sits at the curb</h3>
        <p>Eddie hangs in the down position. Low profile. Easy to miss from the house, which is the point.</p>
      </div>
    </div>
    <div class="step">
      <div class="step-num">02</div>
      <div>
        <h3>Truck flips the bin</h3>
        <p>The lid swings past vertical during the dump. Eddie rotates around his cuff and lands on top of the lid as it falls back closed.</p>
      </div>
    </div>
    <div class="step">
      <div class="step-num">03</div>
      <div>
        <h3>Bin lands. Eddie stays up.</h3>
        <p>You see an orange figure with his arm in the air from 60 feet away. Push him down with one finger when you wheel the bin back. Repeat forever.</p>
      </div>
    </div>
  </div>
</section>

<!-- MECHANISM CARD -->
<section class="mech">
  <div class="wrap">
    <div class="mech-card">
      <div class="photo">
        <img src="{{ 'cuff-attached.jpeg' | asset_url }}" alt="Close-up of the orange cuff seated on the hinge bar" loading="lazy" />
      </div>
      <div class="body">
        <div class="eyebrow">What&rsquo;s actually inside</div>
        <h3>A cuff. A pivot. A stop.</h3>
        <p>The cuff snaps onto the hinge bar of the bin and grips with a foam insert. Eddie&rsquo;s foot ring slips around the cuff and rotates freely.</p>
        <p>When the lid hits a steep enough angle during pickup, Eddie passes over the pivot point and falls flat on top of the lid. A small ledge on the cuff catches him there. No springs. No magnets. Nothing to wear out.</p>
      </div>
    </div>
  </div>
</section>

<!-- BINS -->
<section class="bins">
  <div class="bins-inner">
    <div class="eyebrow">Pick the right cuff</div>
    <h2 class="display">Match your bin.</h2>
    <p class="lead">Look at your bin from the side. The pack ships with the right cuff so you don&rsquo;t have to guess at the store.</p>

    <div class="bins-grid">
      <div class="bin a">
        <div class="photo"><img src="{{ 'RaisedBin.jpeg' | asset_url }}" alt="Bin A side profile, lid sits higher than the handle" loading="lazy" /></div>
        <div class="body">
          <span class="tag">BIN A</span>
          <h3>Lid sits higher than the handle</h3>
          <div class="test">Visible gap between the closed lid and the handle bar below it.</div>
          <div class="pick"><span class="cuff-swatch gray"></span> Use the gray cuff</div>
        </div>
      </div>

      <div class="bin b">
        <div class="photo"><img src="{{ 'LidLevel.jpeg' | asset_url }}" alt="Bin B side profile, lid sits flush with the handle" loading="lazy" /></div>
        <div class="body">
          <span class="tag">BIN B</span>
          <h3>Lid sits level with the handle</h3>
          <div class="test">Closed lid lines up flush with the top of the handle. No gap.</div>
          <div class="pick"><span class="cuff-swatch black"></span> Use the black cuff</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- OUTRO -->
<section class="outro">
  <div class="wrap">
    <h2 class="display">That&rsquo;s <span class="it">Eddie.</span></h2>
    <p>Take him home. He&rsquo;ll be doing his job by next pickup.</p>
    <div class="links">
      <a href="{{ shop.url }}" class="btn btn-primary">See the full site</a>
      <span class="btn btn-ghost is-disabled" aria-disabled="true" role="button" tabindex="-1">Install instructions <small>(coming soon)</small></span>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="foot-logo">Ready Eddie</div>
  <div>Designed and printed in Apex, NC.</div>
  <div class="small">&copy; {{ 'now' | date: '%Y' }} &middot; Patent pending</div>
</footer>

<script>
  (function() {
    var stage = document.getElementById('stage');
    var btn = document.getElementById('toggle');
    var label = btn.querySelector('.label');
    var arrow = btn.querySelector('.arrow');

    btn.addEventListener('click', function() {
      var current = stage.getAttribute('data-state');
      if (current === 'down') {
        stage.setAttribute('data-state', 'up');
        label.textContent = 'Tap to send the truck';
        arrow.innerHTML = '&darr;';
      } else {
        stage.setAttribute('data-state', 'down');
        label.textContent = 'Tap to put it back';
        arrow.innerHTML = '&uarr;';
      }
    });
  })();
</script>

{{ content_for_footer }}

</body>
</html>
